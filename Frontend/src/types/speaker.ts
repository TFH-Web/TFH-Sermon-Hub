/* This file defines the interface for the speaker data structures used in the Speakers component. It includes properties name, role, and sermoncount. */
export interface Speaker {
	name: string;
	role: string;
	id: string;
	sermoncount: string;
	color: string;
}
